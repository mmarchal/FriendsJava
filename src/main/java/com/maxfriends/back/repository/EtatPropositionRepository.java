package com.maxfriends.back.repository;

import com.maxfriends.back.entity.EtatProposition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EtatPropositionRepository extends JpaRepository<EtatProposition, Long> {
}
